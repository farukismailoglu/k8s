apiVersion: apps/v1
kind: Deployment
metadata:
  name: grpc-rental-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grpc-rental-server
  template:
    metadata:
      labels:
        app: grpc-rental-server
    spec:
      containers:
        - name: grpc-rental-server
          image: farukismailoglu/grpc-rental-server
          resources:
            limits:
              memory: "128Mi"
              cpu: "500m"
          ports:
            - containerPort: 50051
              name: grpc